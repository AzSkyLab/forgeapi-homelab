# 12. Ranked questions and verification register

**Status:** all environment experiments **not run**. Owners below are proposed role owners, not confirmed assignments. [Index](README.md).

Documented support is recorded in [references](references.md); a linked product page is not environment evidence. Test result records must identify date, runner, exact versions/configuration, authorized target, inputs, command/procedure, output/evidence digests, actual outcome, outstanding cleanup and reviewer. No numeric acceptance target is invented from a product default.

## Ranked decisions and experiments

Priority P0 blocks the indicated gate, not all independent work. P1 is required before M3 acceptance. P2 is a later milestone decision or optional preference.

| ID / rank / gate | Question and proposed default | Proposed owner | Exact evidence required / fallback |
| --- | --- | --- | --- |
| V01 / P0 / M1 contract freeze | Which immutable Zalando revision and internal lint rules? Recommend section 4's explicit 202/input/enum/permission decisions | Both engineers + API standards owner | Record approved guideline commit/profile; run pinned local structure/examples lint and enterprise rules, review declared deviations. Fallback: no contract freeze; only independently approved draft work proceeds |
| V02 / P0 / connected M1 | Which Entra registration/audience/scopes/roles and approved human PKCE helper? Default enterprise tenant and API-audience tokens | Identity team + API engineer | Real development token, exact curl onboarding, S01/S02 negative/rotation/expiry/overage matrix; CI subject test if CI is enabled. Local signed fixtures do not close connected acceptance; if access unavailable report local core ready only |
| V03 / P0 / connected M1 | Which existing Temporal versions, namespace authorizer and mTLS pattern? Default isolated dev/prod namespaces | Temporal team + provider engineer | Version/config export; laptop-to-dev TLS and denied start/signal/poll/history/reset/terminate/admin matrix; cert overlap/invalid-cert proof. ACA path is M2 V14/V15. Worker Versioning, if selected later, needs server ≥1.29.1, Go SDK ≥1.35.0 and required CLI/UI/config per references. Fallback: local fixtures, no connected completion claim |
| V04 / P0 / M1 completion | Does durable acceptance survive the declared crash/failure matrix? Default outbox, persistent fake and explicit recovery rules | Both engineers | Exact local and connected cases in section 11's tier matrix, including transient DB/result exhaustion, never-started expiry, cancellation-budget failure, Start conflict, Continue-As-New and late effects; record invocation counts/IDs/events and replay. F17 full restore is M3 V13. Missing required fixture/evidence leaves M1 incomplete |
| V05 / P0 / M2.0 deploy | Approved target, allocation mode, actual quotas and service/worker permissions? Default dedicated nonproduction scope; compare user-subscription with Batch-service mode | Azure platform/network teams | Target/plan approval, account/pool/job/core/IP quota inventory, management/data-plane action lists including finite identity assign scopes, Batch service/KV prerequisites and network tests. Fallback: approved Batch-service mode or defer; no silent target/region expansion |
| V06 / P0 / M2.1 before untrusted work | Does supervisor-only Docker plus one-use identity/claim/transfer scope prevent credential and cross-assignment access? Secret delivery disabled initially | Security + identity + provider engineer | S03–S05 and S11 if enabling secrets; root-owned UID/network enforcement, actual rootless packet path, credential inventory, assignment/claim replay/ack-loss tests, scope/expiry/revocation and no cross-execution listing. Alternate broker/session/runtime needs its own approved proof; otherwise no live untrusted run |
| V07 / P0 / M2.2 Batch gate | Does the chosen template need direct untrusted Docker calls? Recommend supervisor-run pinned Compose, tests using helper network, no builds/host privilege | Both engineers + provider engineer | Record exact deterministic tests, named SBOM tool/output, accepted/denied Docker operations, resource caps, requeue/node-loss prelaunch denial and workspace cleanup. M2.1 supplies controls before these probes. If direct calls are required, scope broker/nested-runtime work first; do not quietly weaken the demo |
| V08 / P0 / Batch gate | New VM versus verified reimage and what proves cleanup? Default fresh, single-use VM then deletion | Security + Azure platform | F08–F12/F15: delete during allocation/run, lost delete acknowledgement, delayed submit/allocation, credential expiry, independent inventory and maximum lifetime. No same-node reassignment; failed cleanup is visible. Reimage requires separate explicit evidence/decision |
| V09 / P0 / M2.1 image admission | Which verifier, signing roots, approval owners and patch/revocation policy? Prefer enterprise verifier; Cosign candidate otherwise | Worker/workload image owners + security | Pin OS/kernel/Docker/Compose/node-agent/Gallery/OCI/verifier versions; signer identity/issuer or public-key trust, scan/SBOM/attestation criteria, entrypoint/helper verification, rotation/revocation/fail-closed S06. Reuse approved image pipeline if available; missing enforcement blocks live tests |
| V10 / P0 / live data | Which classification, residency, source/log/history/artifact limits/retention, vault topology and RPO/RTO? Default synthetic test data, approved internal source, section 9 proposals | Data/compliance + DB/storage owners | Approve class/location/bytes/durations and all backup/export copies; artifact extraction/redaction/deletion and restore tests S10; record vault/identity scopes. If undecided, fake/synthetic local evidence only, no regulated data |
| V11 / P0 / before paid M2 work | Agree concurrency, pending backlog, deadlines, startup/cleanup thresholds and experiment spend ceiling | Manager + requesting engineer + FinOps | Record values/rates/sample plan, conservative reservation and accrued-usage rules, stale-monitor/limit denial and operator stop policy; test concurrency/unknown-cleanup exposure with synthetic rates, then S08 measurements. No paid allocation with unset limits or disabled monitor; no performance acceptance without thresholds |
| V12 / P1 / M3 | Which OTLP backend, evidence index and SIEM path? Default one approved collector/backend, sanitized local M1 evidence | Observability/security teams + both engineers | S07 from execution ID, all required stages, failure/redaction canaries, retry/orphan metrics with bounded cardinality; validate export identity and data residency. Full SIEM/HA may remain deferred; minimum evidence cannot |
| V13 / P1 / M3 | Do DB/object/Temporal restore and Entra reconnection meet agreed objectives? Default freeze/compare/quarantine/resume | DB/storage/Temporal owners | F17, measured recovery time/loss, expired token/role/private DNS restoration, unequal restore-point reconciliation and no duplicate workload. Keep recovery claim unverified until demonstrated |
| V14 / P0 / M2.0 hosting | Which approved ACA/internal ingress path and role deployment scopes? Default internal path, no extra gateway | Network/ACA/ingress owners + API engineer | Actual API/worker/runtime app boundaries, TLS/mTLS to dev Temporal, private probes/DB/DNS, auth headers and long-poll routing/no-bypass tests. Fallback: mandated Cloudflare/F5 route; APIM only if existing standard. No approved path means hosting gate pending |
| V15 / P0 / M2.0 hosting | Does deployed ACA shutdown/revision behavior preserve durable work? Default fixed warm replicas and short calls | ACA/Temporal owners + provider engineer | SIGTERM/revision kill mid-effect, actual grace period, drain/concurrency/recovery and cert rotation on hosted roles. M1 process-kill tests are required local evidence, not ACA proof. If drain fails, fix call budgets/recovery before hosting acceptance |
| V16 / P2 / optional CI demo | Which repository/commit and GitHub access pattern for packaging/Checks? Default source artifact published upstream, no GitHub token on worker | Repository owner + identity/CI team | Immutable archive/commit digest, private cross-repo token permission/lifetime test, Check write on permitted event only, fork PR rejection. If unapproved omit Check and use synthetic/prepublished source; CI OAuth integration not core POC |
| V17 / P2 / M4+ price gate | Which estimator/rate source/deployment and policy-compatible auth? Default evaluate Infracost + EA/MCA price books before custom integration | FinOps + vendor/security owner | Representative resource/plan/rate mapping, coverage/currency/tier/effective-date tests, billing permissions, vendor licensing/auth/feature confirmation and observed egress. No key exception assumed; approved rate snapshot/manual bound review in controlled scope if needed |
| V18 / P2 / M5 decisions | Supported manager lookup/sync and CI sponsor/delegation; Teams/email authority? Default approved directory feed | Directory + budget owners + identity team | Graph tenant query/permission test if chosen, stale/missing manager/delegate/self-approval/expiry/revocation cases, authenticated API decisions and deduplicated signal/notification delivery. No background managed-identity manager lookup assumed |
| V19 / P2 / M4 executor | Which exact Terraform/provider/module/Go library versions, plan validity and RBAC delegation? Default single pinned Terraform, immutable bundles and refreshed plans | Platform/pattern owners + both engineers | Offline init, complete transitive digests, Entra backend lock tests, sensitive/write-only resource proof, uncertain apply/state-write recovery and plan/approval invalidation; no OpenTofu equivalence claim |
| V20 / P0 / M1.1 repository and tests | Required Git/owners, development instructions and layered test tooling? Recommend working agreement's task/test sections | Both engineers | Restore usable Git without erasing existing data; assign real CODEOWNERS; create concise AGENTS.md/contributing guide/shared commands after approval; pin and reproduce Go/Temporal/PostgreSQL/OpenAPI checks in Dev Container/CI. Missing prerequisites block affected collaboration/tooling work; no fabricated owners or successful skips |

“Signed” or “approved” evidence in the table describes a future requirement, not a signature already supplied. Each gate records accept/reject/pending against actual results and names any accepted exceptions; security/auth denials cannot be overruled by cost approval.

## Optional preferences and later scaling

| ID / rank / gate | Question / default | Proposed owner | Evidence / fallback |
| --- | --- | --- | --- |
| V21 / P2 / optional development preference | Requested working agreement's agent roles/tool preferences; coordinator alone by default | Both engineers | Record explicit delegation policy, actual client limits, instruction loading and nonoverlapping file/worktree/test ownership if enabled. No agreement or unavailable helpers means work coordinator-only; not an M1 acceptance blocker |
| V22 / P2 / optional later scaling | Is the actual ACA Temporal KEDA scaler available with approved mTLS and safe scale-in? | ACA/Temporal owners | Verify managed-service version/type/auth/metrics and running-work scale-in tests. Fixed replicas remain the supported fallback; no M1/M2 autoscaler prerequisite |

## All source open questions, individually reconciled

| Source §23 | Disposition | Register / default |
| --- | --- | --- |
| Q1 exact Docker features | Still open; concrete proposed subset documented | V07 |
| Q2 all code untrusted? | Combined prompt requires untrusted workload treatment; apply to internal repos too | V06; no trusted-repo bypass |
| Q3 fresh VM or reimage? | Fresh single-use/removal baseline; reimage not accepted | V08 |
| Q4 compelling startup latency? | Still open; no invented threshold | V11 |
| Q5 demonstrated concurrency? | Still open; live deployment needs explicit cap before tests | V11 |
| Q6 cleanup window? | Still open; terminal result/cleanup separate | V08/V11 |
| Q7 allowed classification? | Still open; propose synthetic/nonproduction only initially | V10 |
| Q8 storage/residency/retention for all data? | Proposed record-specific tiers; owner approval pending | V10/V12/V13 |
| Q9 execution-scoped GitHub access? | Prefer upstream source package, optional short-lived approved integration | V06/V16 |
| Q10 available Temporal service? | Resolved by supplied fact: enterprise AKS service; access not verified | V03 |
| Q11 authoritative Zalando/lint versions? | Still open; public docs checked, local proposal not approved | V01 |
| Q12 external ingress required? | Still open; recommend internal-only POC if permitted | V14 |
| Q13 worker-image owner? | Still open, role proposed rather than person invented | V09 |
| Q14 evidence before execution tests? | Lightweight threat model plus identity/network/image/classification approval and adversarial test plan | V05–V10; security owner sets acceptance |
| Q15 cost reporting granularity? | Propose execution marginal cost + shared/idle breakdown with basis/uncertainty | V11/V17 |

## Evidence record and execution order

After authorization, create one evidence record per experiment, not a checkbox changed from “verify” to “done.” Suggested record fields: `verification_id`, `status` (`not_run`, `blocked`, `failed`, `passed`), `milestone`, `authorized_scope_ref`, `owner`, `performed_at`, `versions`, `config_digest`, `procedure`, `sample_count`, `expected`, `observed`, `artifact_refs`, `open_cleanup_refs`, `reviewer`. Keep raw restricted evidence outside public docs; record sanitized references/hashes here.

Order: record M0 scoped decisions → V01 contract freeze and V20 tooling with approved independent M1 work → local core plus V02/V03 connected checks → M1 acceptance V04 → separate M2 authorization and V11 paid-work limits → M2.0 hosting V05/V14/V15 → M2.1 safety/image/data V06/V09/V10 → M2.2 Batch V07/V08 → recorded provider decision → separately approved M3 all-case/live-data/recovery acceptance. V17–V19 precede their later features; V21/V22 are optional and never silently block core work.
